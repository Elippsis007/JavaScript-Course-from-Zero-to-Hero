// An expression is a piece of code that produces a value.

/* 
    <--- Expression --->    

    3 + 4 (expression) and the (value) = 7

    1991 is also an expression because in JS it will produce a value

    true && false && !false -> This is also an expression because it will produce
    a boolean value

   <--- Statement --->

   A statement is like a bigger piece of code that is executed 
   and which DOES NOT produce a value on itself.

*/

/* This block of code is a statement it doesn't produce a value it is
   a declaration, it performs some actions but it doesn't produce a value */

if (23>10) {
    const str = '23 is bigger';
}