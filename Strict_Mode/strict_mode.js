// Strict mode is a special mode in JS that allws people  to make it easier to write secure JS code
// To enable "Strict Mode" for JS I need to type 'use strict'; at the top of each .js script file

'use strict';

let hasDriversLicense = false;
const passTest = true;

if (passTest) hasDriversLicense = true;
if (hasDriversLicense) console.log('I can drive!');

// const interface = 'Audio';

// const private = 534;
